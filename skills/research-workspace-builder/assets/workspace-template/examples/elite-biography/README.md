# Elite-biography example

This directory preserves the original elite-search setup as a demonstration only. `instruction.md` is the combined runnable contract; `instructions.md` and `codebook.json` retain its component sources for comparison. The framework does not load the example unless the builder is invoked with `--example elite-biography` or a user copies it deliberately.
